name: Build docs

on:
  push:
    branches: [ main, development ]

jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: show infos & make build
        run: |
          node -v
          yarn -v
          make -v
          make build
          git status

          